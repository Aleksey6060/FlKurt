import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App smoke test placeholder', (WidgetTester tester) async {
    // Полноценные тесты требуют Firebase mock — пропускаем smoke test
    expect(true, isTrue);
  });
}
